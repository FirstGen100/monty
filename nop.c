#include "monty.h"

/**
 * _nop - doesn't do anythinhg
 *
 * @doubly: head of the linked list
 *count: counter var
 * Return: no return
 */
void _nop(stack_t **doubly, unsigned int count)
{
	(void)doubly;
	(void)count;
}

